import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const Custumize = () => {
  return (
    <View>
      <Text>Tùy chỉnh</Text>
    </View>
  )
}

export default Custumize

const styles = StyleSheet.create({})